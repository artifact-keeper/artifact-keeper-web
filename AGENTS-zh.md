# 代理指令

本项目使用 **bd**（beads）进行问题跟踪。运行 `bd onboard` 即可开始。

## 快速参考

```bash
bd ready              # 查找可认领的工作
bd show <id>          # 查看问题详情
bd update <id> --status in_progress  # 认领工作
bd close <id>         # 完成工作
bd sync               # 与 git 同步
```

## 发起拉取请求（PR）

**每个 PR 都必须在同一个 PR 中为 `CHANGELOG.md` 添加条目。** 一个触达用户却未在变更日志中留下痕迹的改动，在发布时是不可见的；而事后根据合并后的 diff 去还原，远比在思路还清晰时写下来困难得多。

**强制工作流程：**

1. **在 `## [Unreleased]` 下添加条目**，绝不要放在已发布版本的标题下 —— 那些是在发布时才裁剪的。如果 `[Unreleased]` 块不存在，请创建它。
2. **选择与改动匹配的章节**：`Added`、`Changed`、`Fixed`、`Accessibility`、`Security` 或 `Notes`。如果该章节尚不存在，请添加它。
3. **插入到该章节的最顶部** —— 条目按最新在前排列。
4. **遵循统一格式**，每条占一行：
   ```markdown
   - **简短加粗摘要** (#ISSUE) - 用户看到了什么、为什么发生、改了什么。
   ```
5. **引用问题（ISSUE）编号而非 PR 编号** —— `(#768)`，而不是关闭它的那个 PR。变更日志的读者是在追踪他们报告或遇到的某个问题，而他们手里持有的正是这个编号。每个 PR 都有一个，因为 `Require Linked Issue`（要求关联问题）检查强制执行这一点。后端仓库中的问题使用全名引用：`artifact-keeper#2520`。
6. **先写用户可见的症状，再写机制。** 决定是否升级的人必须无需阅读 diff 就能判断这件事是否影响到了他们。
7. **记录你所接受的权衡取舍** —— 已知的成本、有意的范围限制，或遗留的坑，都应写进条目。这正是日后这份变更日志值得一读的原因。

**关键规则：**

- 条目必须与改动处于同一个 PR 中 —— 绝不能"在后续补充"
- 不要编造新的版本标题；新工作放在 `[Unreleased]` 之下
- 不要仅根据 diff 来撰写条目 —— 要描述症状与原因，而非改动的文件
- 真正对用户不可见的改动（内部重构、仅测试、CI）**可以**跳过条目，但你必须在 PR 描述中明确说明这一点，而不是默默地省略

## 落地收尾（会话结束）

**结束一次工作会话时**，你必须完成以下所有步骤。在 `git push` 成功之前，工作不算完成。

**强制工作流程：**

1. **为剩余工作建立问题** - 为任何需要后续跟进的事项创建问题
2. **运行质量门禁**（若代码有改动）- 测试、Lint、构建
3. **更新问题状态** - 关闭已完成的工作，更新进行中的条目
4. **推送到远程仓库** - 这是强制步骤：
   ```bash
   git pull --rebase
   bd sync
   git push
   git status  # 必须显示 "up to date with origin"
   ```
5. **清理** - 清除 stash，修剪远程分支
6. **验证** - 所有改动均已提交并推送
7. **交接** - 为下一次会话提供上下文

**关键规则：**

- 在 `git push` 成功之前，工作不算完成
- 绝不要在推送前停止 —— 那会把工作滞留在本地
- 绝不要说"等你准备好再推送" —— **你**必须推送
- 如果推送失败，请解决并重试，直到成功
